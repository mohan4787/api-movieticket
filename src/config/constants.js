const Status = {
    ACTIVE: "active",
    INACTIVE: "inactive",
}

const USER_ROLES = {
    ADMIN: "admin",
    CUSTOMER: "customer",
}

const GENDER = {
    MALE: "male",
    FEMALE: "female",
    OTHER: "other"
}

module.exports = {
    Status,
    USER_ROLES,
    GENDER
}